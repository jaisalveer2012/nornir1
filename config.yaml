---

inventory:
    plugin: SimpleInventory
    options:
        host_file: "/root/nornir1/hosts.yaml"
        group_file: "groups.yaml"
        defaults_file: "defaults.yaml"

runner:
    plugin: threaded
    options:
        num_workers: 10

